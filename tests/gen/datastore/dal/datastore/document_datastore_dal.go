// Code generated by protoc-gen-dal-datastore. DO NOT EDIT.
package dal

import (
	"context"

	dslib "cloud.google.com/go/datastore"
	datastore "github.com/panyam/protoc-gen-dal/tests/gen/datastore/datastore"
)

// DocumentDatastoreEmptyDAL provides database access helper methods for datastore.DocumentDatastoreEmpty.
type DocumentDatastoreEmptyDAL struct {
	// Kind overrides the Datastore kind for all operations.
	// If empty, uses the struct's Kind() method (if any).
	Kind string

	// Namespace overrides the Datastore namespace for all operations.
	// If empty, uses the default namespace.
	Namespace string

	// WillPut hook is called before Put operations.
	// Return an error to prevent the put.
	WillPut func(context.Context, *datastore.DocumentDatastoreEmpty) error
}

// NewDocumentDatastoreEmptyDAL creates a new DocumentDatastoreEmptyDAL instance.
// If kind is empty, operations will use the struct's Kind() method.
func NewDocumentDatastoreEmptyDAL(kind string) *DocumentDatastoreEmptyDAL {
	return &DocumentDatastoreEmptyDAL{Kind: kind}
}

// getKind returns the kind to use for operations.
// Uses the DAL's Kind field if set, otherwise falls back to the struct's Kind() method.
func (d *DocumentDatastoreEmptyDAL) getKind() string {
	if d.Kind != "" {
		return d.Kind
	}
	// Fall back to struct's Kind() method
	var entity datastore.DocumentDatastoreEmpty
	return entity.Kind()
}

// newKey creates a new Datastore key for the given ID.
func (d *DocumentDatastoreEmptyDAL) newKey(id string) *dslib.Key {
	key := dslib.NameKey(d.getKind(), id, nil)
	if d.Namespace != "" {
		key.Namespace = d.Namespace
	}
	return key
}

// newIncompleteKey creates a new incomplete Datastore key (for auto-generated IDs).
func (d *DocumentDatastoreEmptyDAL) newIncompleteKey() *dslib.Key {
	key := dslib.IncompleteKey(d.getKind(), nil)
	if d.Namespace != "" {
		key.Namespace = d.Namespace
	}
	return key
}

// Put saves a datastore.DocumentDatastoreEmpty entity to Datastore.
// If the entity's Key field is set, uses that key; otherwise creates a key from the ID field.
// Returns the key used to store the entity.
func (d *DocumentDatastoreEmptyDAL) Put(ctx context.Context, client *dslib.Client, obj *datastore.DocumentDatastoreEmpty) (*dslib.Key, error) {
	// Call WillPut hook if set
	if d.WillPut != nil {
		if err := d.WillPut(ctx, obj); err != nil {
			return nil, err
		}
	}

	// Determine the key to use
	var key *dslib.Key
	if obj.Key != nil {
		key = obj.Key
		// Apply namespace override if set
		if d.Namespace != "" {
			key.Namespace = d.Namespace
		}
	} else {
		key = d.newIncompleteKey()
	}

	// Put the entity
	resultKey, err := client.Put(ctx, key, obj)
	if err != nil {
		return nil, err
	}

	// Update the entity's key
	obj.Key = resultKey

	return resultKey, nil
}

// Get retrieves a datastore.DocumentDatastoreEmpty entity by key.
// Returns (nil, nil) if the entity is not found.
func (d *DocumentDatastoreEmptyDAL) Get(ctx context.Context, client *dslib.Client, key *dslib.Key) (*datastore.DocumentDatastoreEmpty, error) {
	var entity datastore.DocumentDatastoreEmpty
	err := client.Get(ctx, key, &entity)
	if err != nil {
		if err == dslib.ErrNoSuchEntity {
			return nil, nil
		}
		return nil, err
	}
	entity.Key = key
	return &entity, nil
}

// Delete removes a datastore.DocumentDatastoreEmpty entity by key.
func (d *DocumentDatastoreEmptyDAL) Delete(ctx context.Context, client *dslib.Client, key *dslib.Key) error {
	return client.Delete(ctx, key)
}

// GetMulti retrieves multiple datastore.DocumentDatastoreEmpty entities by keys.
// Returns entities in the same order as the keys. Missing entities are nil in the result slice.
func (d *DocumentDatastoreEmptyDAL) GetMulti(ctx context.Context, client *dslib.Client, keys []*dslib.Key) ([]*datastore.DocumentDatastoreEmpty, error) {
	if len(keys) == 0 {
		return []*datastore.DocumentDatastoreEmpty{}, nil
	}

	entities := make([]datastore.DocumentDatastoreEmpty, len(keys))
	err := client.GetMulti(ctx, keys, entities)
	if err != nil {
		// Handle partial errors (some entities not found)
		if multiErr, ok := err.(dslib.MultiError); ok {
			result := make([]*datastore.DocumentDatastoreEmpty, len(keys))
			for i, e := range multiErr {
				if e == nil {
					entities[i].Key = keys[i]
					result[i] = &entities[i]
				} else if e != dslib.ErrNoSuchEntity {
					return nil, err // Return on non-NotFound errors
				}
				// nil for not-found entities
			}
			return result, nil
		}
		return nil, err
	}

	// All entities found
	result := make([]*datastore.DocumentDatastoreEmpty, len(keys))
	for i := range entities {
		entities[i].Key = keys[i]
		result[i] = &entities[i]
	}
	return result, nil
}

// PutMulti saves multiple datastore.DocumentDatastoreEmpty entities to Datastore.
// Returns the keys used to store the entities.
func (d *DocumentDatastoreEmptyDAL) PutMulti(ctx context.Context, client *dslib.Client, objs []*datastore.DocumentDatastoreEmpty) ([]*dslib.Key, error) {
	if len(objs) == 0 {
		return []*dslib.Key{}, nil
	}

	// Call WillPut hook for each entity
	if d.WillPut != nil {
		for _, obj := range objs {
			if err := d.WillPut(ctx, obj); err != nil {
				return nil, err
			}
		}
	}

	// Build keys for each entity
	keys := make([]*dslib.Key, len(objs))
	for i, obj := range objs {
		if obj.Key != nil {
			keys[i] = obj.Key
			if d.Namespace != "" {
				keys[i].Namespace = d.Namespace
			}
		} else {
			keys[i] = d.newIncompleteKey()
		}
	}

	// Put all entities
	resultKeys, err := client.PutMulti(ctx, keys, objs)
	if err != nil {
		return nil, err
	}

	// Update entity keys
	for i, key := range resultKeys {
		objs[i].Key = key
	}

	return resultKeys, nil
}

// DeleteMulti removes multiple datastore.DocumentDatastoreEmpty entities by keys.
func (d *DocumentDatastoreEmptyDAL) DeleteMulti(ctx context.Context, client *dslib.Client, keys []*dslib.Key) error {
	if len(keys) == 0 {
		return nil
	}
	return client.DeleteMulti(ctx, keys)
}

// Query retrieves datastore.DocumentDatastoreEmpty entities matching the query.
// The caller should create a query using dslib.NewQuery(dal.getKind()).
func (d *DocumentDatastoreEmptyDAL) Query(ctx context.Context, client *dslib.Client, q *dslib.Query) ([]*datastore.DocumentDatastoreEmpty, error) {
	var entities []*datastore.DocumentDatastoreEmpty
	keys, err := client.GetAll(ctx, q, &entities)
	if err != nil {
		return nil, err
	}

	// Set keys on entities
	for i, key := range keys {
		entities[i].Key = key
	}

	return entities, nil
}

// Count returns the number of entities matching the query.
func (d *DocumentDatastoreEmptyDAL) Count(ctx context.Context, client *dslib.Client, q *dslib.Query) (int, error) {
	return client.Count(ctx, q)
}

// DocumentDatastorePartialDAL provides database access helper methods for datastore.DocumentDatastorePartial.
type DocumentDatastorePartialDAL struct {
	// Kind overrides the Datastore kind for all operations.
	// If empty, uses the struct's Kind() method (if any).
	Kind string

	// Namespace overrides the Datastore namespace for all operations.
	// If empty, uses the default namespace.
	Namespace string

	// WillPut hook is called before Put operations.
	// Return an error to prevent the put.
	WillPut func(context.Context, *datastore.DocumentDatastorePartial) error
}

// NewDocumentDatastorePartialDAL creates a new DocumentDatastorePartialDAL instance.
// If kind is empty, operations will use the struct's Kind() method.
func NewDocumentDatastorePartialDAL(kind string) *DocumentDatastorePartialDAL {
	return &DocumentDatastorePartialDAL{Kind: kind}
}

// getKind returns the kind to use for operations.
// Uses the DAL's Kind field if set, otherwise falls back to the struct's Kind() method.
func (d *DocumentDatastorePartialDAL) getKind() string {
	if d.Kind != "" {
		return d.Kind
	}
	// Fall back to struct's Kind() method
	var entity datastore.DocumentDatastorePartial
	return entity.Kind()
}

// newKey creates a new Datastore key for the given ID.
func (d *DocumentDatastorePartialDAL) newKey(id string) *dslib.Key {
	key := dslib.NameKey(d.getKind(), id, nil)
	if d.Namespace != "" {
		key.Namespace = d.Namespace
	}
	return key
}

// newIncompleteKey creates a new incomplete Datastore key (for auto-generated IDs).
func (d *DocumentDatastorePartialDAL) newIncompleteKey() *dslib.Key {
	key := dslib.IncompleteKey(d.getKind(), nil)
	if d.Namespace != "" {
		key.Namespace = d.Namespace
	}
	return key
}

// Put saves a datastore.DocumentDatastorePartial entity to Datastore.
// If the entity's Key field is set, uses that key; otherwise creates a key from the ID field.
// Returns the key used to store the entity.
func (d *DocumentDatastorePartialDAL) Put(ctx context.Context, client *dslib.Client, obj *datastore.DocumentDatastorePartial) (*dslib.Key, error) {
	// Call WillPut hook if set
	if d.WillPut != nil {
		if err := d.WillPut(ctx, obj); err != nil {
			return nil, err
		}
	}

	// Determine the key to use
	var key *dslib.Key
	if obj.Key != nil {
		key = obj.Key
		// Apply namespace override if set
		if d.Namespace != "" {
			key.Namespace = d.Namespace
		}
	} else if obj.Id != "" {
		key = d.newKey(obj.Id)
	} else {
		key = d.newIncompleteKey()
	}

	// Put the entity
	resultKey, err := client.Put(ctx, key, obj)
	if err != nil {
		return nil, err
	}

	// Update the entity's key
	obj.Key = resultKey

	return resultKey, nil
}

// Get retrieves a datastore.DocumentDatastorePartial entity by key.
// Returns (nil, nil) if the entity is not found.
func (d *DocumentDatastorePartialDAL) Get(ctx context.Context, client *dslib.Client, key *dslib.Key) (*datastore.DocumentDatastorePartial, error) {
	var entity datastore.DocumentDatastorePartial
	err := client.Get(ctx, key, &entity)
	if err != nil {
		if err == dslib.ErrNoSuchEntity {
			return nil, nil
		}
		return nil, err
	}
	entity.Key = key
	return &entity, nil
}

// Delete removes a datastore.DocumentDatastorePartial entity by key.
func (d *DocumentDatastorePartialDAL) Delete(ctx context.Context, client *dslib.Client, key *dslib.Key) error {
	return client.Delete(ctx, key)
}

// GetMulti retrieves multiple datastore.DocumentDatastorePartial entities by keys.
// Returns entities in the same order as the keys. Missing entities are nil in the result slice.
func (d *DocumentDatastorePartialDAL) GetMulti(ctx context.Context, client *dslib.Client, keys []*dslib.Key) ([]*datastore.DocumentDatastorePartial, error) {
	if len(keys) == 0 {
		return []*datastore.DocumentDatastorePartial{}, nil
	}

	entities := make([]datastore.DocumentDatastorePartial, len(keys))
	err := client.GetMulti(ctx, keys, entities)
	if err != nil {
		// Handle partial errors (some entities not found)
		if multiErr, ok := err.(dslib.MultiError); ok {
			result := make([]*datastore.DocumentDatastorePartial, len(keys))
			for i, e := range multiErr {
				if e == nil {
					entities[i].Key = keys[i]
					result[i] = &entities[i]
				} else if e != dslib.ErrNoSuchEntity {
					return nil, err // Return on non-NotFound errors
				}
				// nil for not-found entities
			}
			return result, nil
		}
		return nil, err
	}

	// All entities found
	result := make([]*datastore.DocumentDatastorePartial, len(keys))
	for i := range entities {
		entities[i].Key = keys[i]
		result[i] = &entities[i]
	}
	return result, nil
}

// PutMulti saves multiple datastore.DocumentDatastorePartial entities to Datastore.
// Returns the keys used to store the entities.
func (d *DocumentDatastorePartialDAL) PutMulti(ctx context.Context, client *dslib.Client, objs []*datastore.DocumentDatastorePartial) ([]*dslib.Key, error) {
	if len(objs) == 0 {
		return []*dslib.Key{}, nil
	}

	// Call WillPut hook for each entity
	if d.WillPut != nil {
		for _, obj := range objs {
			if err := d.WillPut(ctx, obj); err != nil {
				return nil, err
			}
		}
	}

	// Build keys for each entity
	keys := make([]*dslib.Key, len(objs))
	for i, obj := range objs {
		if obj.Key != nil {
			keys[i] = obj.Key
			if d.Namespace != "" {
				keys[i].Namespace = d.Namespace
			}
		} else if obj.Id != "" {
			keys[i] = d.newKey(obj.Id)
		} else {
			keys[i] = d.newIncompleteKey()
		}
	}

	// Put all entities
	resultKeys, err := client.PutMulti(ctx, keys, objs)
	if err != nil {
		return nil, err
	}

	// Update entity keys
	for i, key := range resultKeys {
		objs[i].Key = key
	}

	return resultKeys, nil
}

// DeleteMulti removes multiple datastore.DocumentDatastorePartial entities by keys.
func (d *DocumentDatastorePartialDAL) DeleteMulti(ctx context.Context, client *dslib.Client, keys []*dslib.Key) error {
	if len(keys) == 0 {
		return nil
	}
	return client.DeleteMulti(ctx, keys)
}

// Query retrieves datastore.DocumentDatastorePartial entities matching the query.
// The caller should create a query using dslib.NewQuery(dal.getKind()).
func (d *DocumentDatastorePartialDAL) Query(ctx context.Context, client *dslib.Client, q *dslib.Query) ([]*datastore.DocumentDatastorePartial, error) {
	var entities []*datastore.DocumentDatastorePartial
	keys, err := client.GetAll(ctx, q, &entities)
	if err != nil {
		return nil, err
	}

	// Set keys on entities
	for i, key := range keys {
		entities[i].Key = key
	}

	return entities, nil
}

// Count returns the number of entities matching the query.
func (d *DocumentDatastorePartialDAL) Count(ctx context.Context, client *dslib.Client, q *dslib.Query) (int, error) {
	return client.Count(ctx, q)
}

// GetByID retrieves a datastore.DocumentDatastorePartial entity by ID.
// This is a convenience method that creates a key from the ID.
// Returns (nil, nil) if the entity is not found.
func (d *DocumentDatastorePartialDAL) GetByID(ctx context.Context, client *dslib.Client, id string) (*datastore.DocumentDatastorePartial, error) {
	key := d.newKey(id)
	return d.Get(ctx, client, key)
}

// DeleteByID removes a datastore.DocumentDatastorePartial entity by ID.
// This is a convenience method that creates a key from the ID.
func (d *DocumentDatastorePartialDAL) DeleteByID(ctx context.Context, client *dslib.Client, id string) error {
	key := d.newKey(id)
	return d.Delete(ctx, client, key)
}

// GetMultiByIDs retrieves multiple datastore.DocumentDatastorePartial entities by IDs.
// This is a convenience method that creates keys from the IDs.
// Returns entities in the same order as the IDs. Missing entities are nil in the result slice.
func (d *DocumentDatastorePartialDAL) GetMultiByIDs(ctx context.Context, client *dslib.Client, ids []string) ([]*datastore.DocumentDatastorePartial, error) {
	if len(ids) == 0 {
		return []*datastore.DocumentDatastorePartial{}, nil
	}

	keys := make([]*dslib.Key, len(ids))
	for i, id := range ids {
		keys[i] = d.newKey(id)
	}

	return d.GetMulti(ctx, client, keys)
}

// DocumentDatastoreSkipDAL provides database access helper methods for datastore.DocumentDatastoreSkip.
type DocumentDatastoreSkipDAL struct {
	// Kind overrides the Datastore kind for all operations.
	// If empty, uses the struct's Kind() method (if any).
	Kind string

	// Namespace overrides the Datastore namespace for all operations.
	// If empty, uses the default namespace.
	Namespace string

	// WillPut hook is called before Put operations.
	// Return an error to prevent the put.
	WillPut func(context.Context, *datastore.DocumentDatastoreSkip) error
}

// NewDocumentDatastoreSkipDAL creates a new DocumentDatastoreSkipDAL instance.
// If kind is empty, operations will use the struct's Kind() method.
func NewDocumentDatastoreSkipDAL(kind string) *DocumentDatastoreSkipDAL {
	return &DocumentDatastoreSkipDAL{Kind: kind}
}

// getKind returns the kind to use for operations.
// Uses the DAL's Kind field if set, otherwise falls back to the struct's Kind() method.
func (d *DocumentDatastoreSkipDAL) getKind() string {
	if d.Kind != "" {
		return d.Kind
	}
	// Fall back to struct's Kind() method
	var entity datastore.DocumentDatastoreSkip
	return entity.Kind()
}

// newKey creates a new Datastore key for the given ID.
func (d *DocumentDatastoreSkipDAL) newKey(id string) *dslib.Key {
	key := dslib.NameKey(d.getKind(), id, nil)
	if d.Namespace != "" {
		key.Namespace = d.Namespace
	}
	return key
}

// newIncompleteKey creates a new incomplete Datastore key (for auto-generated IDs).
func (d *DocumentDatastoreSkipDAL) newIncompleteKey() *dslib.Key {
	key := dslib.IncompleteKey(d.getKind(), nil)
	if d.Namespace != "" {
		key.Namespace = d.Namespace
	}
	return key
}

// Put saves a datastore.DocumentDatastoreSkip entity to Datastore.
// If the entity's Key field is set, uses that key; otherwise creates a key from the ID field.
// Returns the key used to store the entity.
func (d *DocumentDatastoreSkipDAL) Put(ctx context.Context, client *dslib.Client, obj *datastore.DocumentDatastoreSkip) (*dslib.Key, error) {
	// Call WillPut hook if set
	if d.WillPut != nil {
		if err := d.WillPut(ctx, obj); err != nil {
			return nil, err
		}
	}

	// Determine the key to use
	var key *dslib.Key
	if obj.Key != nil {
		key = obj.Key
		// Apply namespace override if set
		if d.Namespace != "" {
			key.Namespace = d.Namespace
		}
	} else if obj.Id != "" {
		key = d.newKey(obj.Id)
	} else {
		key = d.newIncompleteKey()
	}

	// Put the entity
	resultKey, err := client.Put(ctx, key, obj)
	if err != nil {
		return nil, err
	}

	// Update the entity's key
	obj.Key = resultKey

	return resultKey, nil
}

// Get retrieves a datastore.DocumentDatastoreSkip entity by key.
// Returns (nil, nil) if the entity is not found.
func (d *DocumentDatastoreSkipDAL) Get(ctx context.Context, client *dslib.Client, key *dslib.Key) (*datastore.DocumentDatastoreSkip, error) {
	var entity datastore.DocumentDatastoreSkip
	err := client.Get(ctx, key, &entity)
	if err != nil {
		if err == dslib.ErrNoSuchEntity {
			return nil, nil
		}
		return nil, err
	}
	entity.Key = key
	return &entity, nil
}

// Delete removes a datastore.DocumentDatastoreSkip entity by key.
func (d *DocumentDatastoreSkipDAL) Delete(ctx context.Context, client *dslib.Client, key *dslib.Key) error {
	return client.Delete(ctx, key)
}

// GetMulti retrieves multiple datastore.DocumentDatastoreSkip entities by keys.
// Returns entities in the same order as the keys. Missing entities are nil in the result slice.
func (d *DocumentDatastoreSkipDAL) GetMulti(ctx context.Context, client *dslib.Client, keys []*dslib.Key) ([]*datastore.DocumentDatastoreSkip, error) {
	if len(keys) == 0 {
		return []*datastore.DocumentDatastoreSkip{}, nil
	}

	entities := make([]datastore.DocumentDatastoreSkip, len(keys))
	err := client.GetMulti(ctx, keys, entities)
	if err != nil {
		// Handle partial errors (some entities not found)
		if multiErr, ok := err.(dslib.MultiError); ok {
			result := make([]*datastore.DocumentDatastoreSkip, len(keys))
			for i, e := range multiErr {
				if e == nil {
					entities[i].Key = keys[i]
					result[i] = &entities[i]
				} else if e != dslib.ErrNoSuchEntity {
					return nil, err // Return on non-NotFound errors
				}
				// nil for not-found entities
			}
			return result, nil
		}
		return nil, err
	}

	// All entities found
	result := make([]*datastore.DocumentDatastoreSkip, len(keys))
	for i := range entities {
		entities[i].Key = keys[i]
		result[i] = &entities[i]
	}
	return result, nil
}

// PutMulti saves multiple datastore.DocumentDatastoreSkip entities to Datastore.
// Returns the keys used to store the entities.
func (d *DocumentDatastoreSkipDAL) PutMulti(ctx context.Context, client *dslib.Client, objs []*datastore.DocumentDatastoreSkip) ([]*dslib.Key, error) {
	if len(objs) == 0 {
		return []*dslib.Key{}, nil
	}

	// Call WillPut hook for each entity
	if d.WillPut != nil {
		for _, obj := range objs {
			if err := d.WillPut(ctx, obj); err != nil {
				return nil, err
			}
		}
	}

	// Build keys for each entity
	keys := make([]*dslib.Key, len(objs))
	for i, obj := range objs {
		if obj.Key != nil {
			keys[i] = obj.Key
			if d.Namespace != "" {
				keys[i].Namespace = d.Namespace
			}
		} else if obj.Id != "" {
			keys[i] = d.newKey(obj.Id)
		} else {
			keys[i] = d.newIncompleteKey()
		}
	}

	// Put all entities
	resultKeys, err := client.PutMulti(ctx, keys, objs)
	if err != nil {
		return nil, err
	}

	// Update entity keys
	for i, key := range resultKeys {
		objs[i].Key = key
	}

	return resultKeys, nil
}

// DeleteMulti removes multiple datastore.DocumentDatastoreSkip entities by keys.
func (d *DocumentDatastoreSkipDAL) DeleteMulti(ctx context.Context, client *dslib.Client, keys []*dslib.Key) error {
	if len(keys) == 0 {
		return nil
	}
	return client.DeleteMulti(ctx, keys)
}

// Query retrieves datastore.DocumentDatastoreSkip entities matching the query.
// The caller should create a query using dslib.NewQuery(dal.getKind()).
func (d *DocumentDatastoreSkipDAL) Query(ctx context.Context, client *dslib.Client, q *dslib.Query) ([]*datastore.DocumentDatastoreSkip, error) {
	var entities []*datastore.DocumentDatastoreSkip
	keys, err := client.GetAll(ctx, q, &entities)
	if err != nil {
		return nil, err
	}

	// Set keys on entities
	for i, key := range keys {
		entities[i].Key = key
	}

	return entities, nil
}

// Count returns the number of entities matching the query.
func (d *DocumentDatastoreSkipDAL) Count(ctx context.Context, client *dslib.Client, q *dslib.Query) (int, error) {
	return client.Count(ctx, q)
}

// GetByID retrieves a datastore.DocumentDatastoreSkip entity by ID.
// This is a convenience method that creates a key from the ID.
// Returns (nil, nil) if the entity is not found.
func (d *DocumentDatastoreSkipDAL) GetByID(ctx context.Context, client *dslib.Client, id string) (*datastore.DocumentDatastoreSkip, error) {
	key := d.newKey(id)
	return d.Get(ctx, client, key)
}

// DeleteByID removes a datastore.DocumentDatastoreSkip entity by ID.
// This is a convenience method that creates a key from the ID.
func (d *DocumentDatastoreSkipDAL) DeleteByID(ctx context.Context, client *dslib.Client, id string) error {
	key := d.newKey(id)
	return d.Delete(ctx, client, key)
}

// GetMultiByIDs retrieves multiple datastore.DocumentDatastoreSkip entities by IDs.
// This is a convenience method that creates keys from the IDs.
// Returns entities in the same order as the IDs. Missing entities are nil in the result slice.
func (d *DocumentDatastoreSkipDAL) GetMultiByIDs(ctx context.Context, client *dslib.Client, ids []string) ([]*datastore.DocumentDatastoreSkip, error) {
	if len(ids) == 0 {
		return []*datastore.DocumentDatastoreSkip{}, nil
	}

	keys := make([]*dslib.Key, len(ids))
	for i, id := range ids {
		keys[i] = d.newKey(id)
	}

	return d.GetMulti(ctx, client, keys)
}
